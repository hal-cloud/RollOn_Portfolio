//=========================================================
// judgeLine.h
// 判定ラインクラス
//
// ノーツの判定基準となるラインを Field オブジェクトとして表示する。
//=========================================================
#pragma once
#include <array>
#include "main.h"
#include "renderer.h"
#include "gameObject.h"

class JudgeLine : public GameObject
{
    //=========================================================
    // メンバ変数
    //=========================================================
private:
    //-----------------------------------------------------
    // 判定ライン ビジュアル（Scene が所有。ポインタを借りるだけで delete 不可）
    // Uninit() で SetDestroy() を呼び、Scene 側に解放を委ねる
    //-----------------------------------------------------
    class Field* m_judgeLineVisual = nullptr;

    //=========================================================
    // ライフサイクル
    //=========================================================
public:
    //-----------------------------------------------------
    // Init
    // 判定ラインを Field オブジェクトとしてシーンに追加する
    // 位置・色・ガンマを設定して帯として表示する
    //-----------------------------------------------------
    void Init();

    //-----------------------------------------------------
    // Uninit
    // Field の所有権は Scene 側にあるため、
    // SetDestroy() で削除を依頼するだけで delete はしない
    //-----------------------------------------------------
    void Uninit(void) override;
};